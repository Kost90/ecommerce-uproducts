// import { httpCodeHelper } from '@/lib/httpCodeHelper';

// export const errorHandler =
//   (e) =>
//   (useToast): void => {
//     if (e.error?.statusCode === httpCodeHelper.unAuthorized) return;

//     if (e.error?.statusCode === httpCodeHelper.serverError) {
//       useToast({
//         title: 'Error',
//         description: e.error.message,
//       });
//     } else {
//       useToast({
//         title: 'Error',
//         description: e.error.message,
//       });
//       console.error('error: ', e?.message);
//     }
//   };
